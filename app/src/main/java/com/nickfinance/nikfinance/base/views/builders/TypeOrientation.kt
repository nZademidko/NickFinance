package com.nickfinance.nikfinance.base.views.builders

import android.widget.RelativeLayout.*

enum class TypeOrientation(val value: Int) {
    TOP(value = ALIGN_PARENT_TOP),
    CENTER(value = CENTER_VERTICAL),
    BOTTOM(value = ALIGN_PARENT_BOTTOM)
}